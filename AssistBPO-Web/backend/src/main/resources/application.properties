# ===================== CONFIGURAÇÕES DE PORTA =====================
server.port=${PORT:8080}

# ===================== ENCODING CORRETO =====================
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# ===================== LOGGING =====================
logging.level.org.springframework=INFO
logging.level.com.assistbpo=DEBUG

# ===================== DATABASE CONFIG =====================

# --- OPÇÃO 1: H2 (Em Memória - Padrão para teste sem instalar nada) ---
#spring.datasource.url=jdbc:h2:mem:assistbpo
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Habilita console do H2 em /h2-console
#spring.h2.console.enabled=true

# --- OPÇÃO 2: PostgreSQL (Descomente para produção) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/assistbpo
spring.datasource.username=postgres
spring.datasource.password=Ungido@27
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
